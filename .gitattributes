# Keep the checked-out bytes identical on every platform: the fixture CSVs, the
# generated .cir decks and the exported model libraries are all content-hashed by
# the pipeline, so a CRLF-only difference must not change a hash.
* text=auto eol=lf

*.png binary
*.pdf binary
*.raw binary
*.lib binary
*.sub binary
*.exe binary
